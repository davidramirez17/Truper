import { ArrowUpRight, Database, FileSpreadsheet, ShieldCheck } from 'lucide-react';
import { Brand } from '@/components/ui/Brand';
import { AuthForm, type AuthMode } from './AuthForm';

export function AuthShell({ mode, configured, linkError }: { mode: AuthMode; configured: boolean; linkError?: boolean }) {
  return <main className="auth-layout"><section className="auth-story"><div className="auth-story-grid" aria-hidden="true" /><div className="auth-brand"><Brand /><span className="auth-internal">PLATAFORMA DE OPERACIÓN</span></div><div className="auth-story-copy"><span className="auth-kicker"><i /> LA FUERZA DE TRABAJAR MEJOR</span><h2>Grandes resultados.<br />Empiezan con<br /><em>buenas decisiones.</em></h2><p>Transforma tus archivos en información que mueve a tu equipo. Todo tu trabajo, conectado en un mismo lugar.</p><div className="auth-process"><span><FileSpreadsheet size={20} /><b>Tus archivos</b></span><ArrowUpRight size={20} /><span><Database size={20} /><b>Tu información</b></span><ArrowUpRight size={20} /><span><ShieldCheck size={20} /><b>Tu control</b></span></div></div><div className="auth-story-bottom"><span>CONSTRUIDO PARA HACER MÁS.</span><span>TRUPER WORKSPACE / 01</span></div><div className="auth-orange-shape" aria-hidden="true" /></section><section className="auth-entry"><div className="auth-mobile-brand"><Brand /></div><span className="auth-entry-caption">INTELIGENCIA COMERCIAL</span><AuthForm mode={mode} configured={configured} linkError={linkError} /><footer><span>Truper Workspace</span><span>Información clara. Trabajo bien hecho.</span></footer></section></main>;
}
